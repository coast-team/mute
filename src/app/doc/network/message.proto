syntax = "proto3";

message Message {
  string service = 1;
  bytes content = 2;
}

message BotRequest {
  string key = 1;
}

message BotReponse {
  ErrorCode error = 1;

  enum ErrorCode {
    DATABASE = 0;
  }
}
